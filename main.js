const bnfTabs = document.getElementById('bnf-tabs');
const bnfBackward = document.getElementById('bnf-backward');
const bnfForward = document.getElementById('bnf-forward');

function computed(elem , prop){
    return window.getComputedStyle(elem).getPropertyValue(prop);
}

